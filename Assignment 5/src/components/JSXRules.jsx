
import React from 'react';

function JSXRules() {
  return (
    <div>
      <h1>JSX Rules</h1>
      <p>Here are some important rules to follow when using JSX:</p>
      <ul>
        <li>JSX must return a single parent element.</li>
        <li>JSX elements must be properly closed.</li>
        <li>JSX attributes use camelCase (e.g., <code>className</code> instead of <code>class</code>).</li>
      </ul>
    </div>
  );
}

export default JSXRules;
